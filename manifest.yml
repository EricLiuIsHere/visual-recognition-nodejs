---
declared-services:
  visual-recognition-service:
    label: watson_vision_combined
    plan: free
applications:
- name: visual-recognition-eric
  path: .
  command: npm start
  memory: 512M
  services:
  - Visual Recognition-3o
  env:
    NODE_ENV: production
